import { GameMode } from "./types/lol"

export const challengeWithCompletion: { id: string; gameMode: GameMode }[] = [
  {
    // Arena God - ADATTA A TUTTE LE SITUAZIONI
    id: "602002",
    gameMode: "Arena",
  },
]
